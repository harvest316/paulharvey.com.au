.wrangler/
.env
.venv/

# Real-contact "(full)" variants are built OUTSIDE public/ (out of deploy scope).
# Belt-and-braces: ignore them anywhere they might land.
**/Paul Harvey - Resume (full).docx
**/Paul Harvey - Resume (full).pdf

# Python bytecode + intermediate HTML from build-resume-pdf.py --keep-html
__pycache__/
public/cv/docs/*.html

# One-off per-JD tailored resumes + cover letters + private full-contact builds
out/

# Python test + coverage artifacts
.coverage
.pytest_cache/
tests/__pycache__/
